[project]
name = "npc-ecommerce-sandbox"
version = "0.1.0"
description = "NPC E-commerce Sandbox - Multi-agent simulation for e-commerce operations"
readme = "README.md"
requires-python = ">=3.11,<3.14"
license = { text = "MIT" }
authors = [
    { name = "Your Name", email = "your.email@example.com" }
]
keywords = ["crewai", "autogen", "multi-agent", "ecommerce", "simulation"]

dependencies = [
    # Agent frameworks
    "crewai>=1.9.0",
    "crewai-tools>=0.17.0",
    "autogen-agentchat>=0.4.0",
    "autogen-ext[openai,anthropic]>=0.4.0",
    # Web framework
    "fastapi>=0.109.0",
    "uvicorn[standard]>=0.27.0",
    # Database
    "sqlalchemy>=2.0.0",
    "alembic>=1.13.0",
    "asyncpg>=0.29.0",
    "psycopg2-binary>=2.9.0",
    "redis>=5.0.0",
    # UI
    "streamlit>=1.30.0",
    # Core utilities
    "pydantic>=2.5.0",
    "pydantic-settings>=2.1.0",
    "python-dotenv>=1.0.0",
    "httpx>=0.26.0",
    # LLM Providers - OpenAI
    "openai>=1.10.0",
    "langchain-openai>=0.2.0",
    # LLM Providers - Anthropic Claude
    "anthropic>=0.40.0",
    "langchain-anthropic>=0.2.0",
    # LLM Providers - Google Gemini
    "google-generativeai>=0.8.0",
    "langchain-google-genai>=2.0.0",
    # LLM Providers - Local (Ollama)
    "langchain-ollama>=0.2.0",
]

[project.optional-dependencies]
dev = [
    # Testing
    "pytest>=7.4.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.1.0",
    "pytest-mock>=3.12.0",
    "pytest-xdist>=3.5.0",
    "httpx>=0.26.0",
    "aiosqlite>=0.19.0",
    # Linting & Formatting
    "black>=24.1.0",
    "ruff>=0.1.0",
    "mypy>=1.8.0",
    # Security
    "bandit>=1.7.0",
    "safety>=2.3.0",
]

[project.scripts]
npc-sandbox = "main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/npc_ecommerce_sandbox"]

[tool.black]
line-length = 100
target-version = ["py311"]

[tool.ruff]
line-length = 100
target-version = "py311"
select = ["E", "F", "I", "N", "W", "UP"]

[tool.mypy]
python_version = "3.11"
strict = true
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
addopts = "-v --tb=short"
markers = [
    "integration: marks tests as integration tests (deselect with '-m \"not integration\"')",
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
]
filterwarnings = [
    "ignore::DeprecationWarning",
    "ignore::PendingDeprecationWarning",
]

[tool.coverage.run]
source = ["src/npc_ecommerce_sandbox"]
branch = true
omit = [
    "*/tests/*",
    "*/__init__.py",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
]
fail_under = 60
show_missing = true
